# xtuer-zhou 开发文档 / 变更日志

## 2026-07-19 v2 重构收尾（W1 → W2.6 → W3）

### 用户输入

- 用户要求在 D 盘继续 v2 重构工作，先写「重构收尾开发规划」再执行。
- 确认 3 个决策岔口：QA 卡直接跳资料详情、反诈条走简版、card 沿用一内容多归属挂载。
- 逐批推进 W1（首页大改）→ W2（内容 + 校验）→ W2.5（60 条总览链接）→ W2.6（partial 引导语措辞重构）→ W3（清理 + 冒烟 + 归档）。
- 中途指出「文件不齐时的引导语应该是『联系我们获取』而不是『联系我们补充』」，触发 W2.6。

### 本次执行

- **W1 首页 + 跳转打通**：
  - 新增 `product/development-plan-v2-finish.md` 作为收尾执行清单，落地 M1–M5 实测账。
  - 重写 `app/page.tsx`：反诈提醒条（链 `/about#anti-fraud`） + Hero/AskPanel + 时间线 6 卡 + 主题 8 卡 + 高频 Q&A 4 卡 + 咨询 3 卡（全部可点）；移除对 `guideModules` 的引用。
  - `app/globals.css` 新增 `.fraud-banner`、`.fraud-banner-tag`、`.fraud-banner-text`、`.fraud-banner-cta` 及 640px 断点。
  - 实测发现 v2 主规划里 T1–T5/T7/T8 均已在 C 盘阶段落地，本轮仅动首页与 CSS。
- **W2 内容与校验补齐**：
  - 新增 `scripts/check-links.mjs`：扫 `docs/**/*.md` 内的 `[X](/library/...)`，缺失即 `exit(1)`；`package.json` 增加 `check:links` 脚本。
  - 改造 `scripts/build-rag-index.mjs`：新增 `stripInlineMarkdown()` / `parseArrayField()`，`chunk.heading` 前 strip 掉 `**` / `[]()`；chunk 携带 `primarySection / sections / phase / tags` 四字段。
  - 修 5 处坏链接：`docs/life/{医保就医超优惠,寝室生活大揭秘}.md`、`docs/tools/{信息门户使用指南,线上缴费指南}.md` 里 `/library/tools/实用工具小程序` → `/library/life/实用工具小程序`（真实 primarySection 是 life）。
  - 复核 `/about` 四段、`docs/card/新生必看什么是校园卡.md`、`docs/tools/校园一卡通指南.md`，均已在 C 盘阶段成形，本轮未改。
- **W2.5 总览页链接补齐（60 条）**：
  - 修 6 份总览 md：`campus/湘大印象总览.md`（5 条）、`life/校园生活总览.md`（8 条）、`newcomer/新生攻略总览.md`（13 条含 4 跨 section）、`orgs/组织与工作总览.md`（12 条）、`study/学在湘大总览.md`（13 条含 5 跨 section）、`tools/工具资源总览.md`（9 条，删除重复的「校园电话卡详解」条目）。
  - 跨 section 条目附加「（主题：xx）」标签，避免用户跳转时视觉突兀。
  - 决策 A 落实：「奖助政策 Q&A」链到 `tools/奖学金与助学金政策`（内容合并入库）。
- **W2.6 partial 引导语翻转（读者视角）**：
  - 重写 `app/library/[section]/[...slug]/page.tsx` 的 supplement 卡片：标题「想要更完整的信息？可以这样获取」+ 3 条获取路径（加学长学姐微信 / 加入 QQ 新生群 / 学校官方渠道）+ 脚注一行给愿意贡献者。
  - `components/contact-dialog.tsx`、`app/about/page.tsx §letter`、`lib/site-data.ts` 的 contact.note 全部把「补充」措辞翻转成「获取 / 直接问」视角。
  - `app/globals.css` 新增 `.supplement-paths` / `.supplement-path` 三列布局，复用 `.qr-placeholder`。
  - `docs/**/*.md` 里的 `completenessNote` 保留不动——它们是维护者视角的针对性说明，语义正确。
- **W3 清理与验收**：
  - 删除 `lib/site-data.ts` 中不再使用的 `guideModules` / `libraryCategories` 与其类型（首页大改后已解耦）。
  - Playwright 桌面 1280×800 + 移动 360×720 双端冒烟：首页反诈条 / 时间线 / 主题 / Q&A / 咨询、`/library/newcomer/军训指南`（partial 引导块）、`/guide/first-week`（15 篇聚合 + 阶段导航）、`/qa`、`/about`（含 4 段锚点）全部走通。
  - 修 `/qa` 页 eyebrow 硬编码 `4 篇问答文档` → 动态 `{qaDocs.length} 篇问答文档`（实际 3 卡）。

### 验证状态

- `npm run check:links` 通过：66 docs、全部内部 `/library/...` 链接可达。
- `npm run rag:index` 通过：**804 chunks**（v1 762 → 803 → 804，反映 heading 清洗 + 总览页新链接的净变化）。
- `npm run build` 通过：**89 张静态页面**（v1 66 → 89，新增 8 section + 6 phase + /qa + /about 等）。
- Playwright 冒烟未见 404 / 白屏；移动端反诈条 / 时间轴 / 主题网格 / supplement 三卡均正常堆叠。

### 当前状态

v2 主规划的 Phase A–E 全部落地，M1–M5 里程碑全绿。目录、路由、跳转、内容归属、内部链接校验、partial 引导语视角全部对齐 v2 目标态。下一步建议：
1. 在浏览器上做一次真人视角的完整体验（10 分钟），把仍能感知到的措辞 / 排版细节列出来，做一批「打磨型」小修。
2. 部署测试准备（Vercel 或类似平台注入 `DEEPSEEK_API_KEY`，`.env.local` 不入库）。
3. `product/development-roadmap.md` v0.1 的 Phase 目标已被 v2-finish 完全覆盖，可以正式归档 v0.1，让 v2-finish 成为唯一在用的排期。

## 2026-07-18 60 条待整理资料全量入库 + 补充信息引导块上线

### 用户输入

- 用户要求先转入正式库 04 和 37 号资料，自行在网络上完成搜索补充。
- 图片或附件不足的文件在卡片末尾引导用户添加微信联系方式和新生群。
- 整理完成全部文件后从方向一（内容审核入库）推进。

### 本次执行

- **组件层**：在 `lib/markdown.ts` 加入 `completeness` 与 `completenessNote` 字段；在 `app/library/[...slug]/page.tsx` 追加 “**补充信息**” 引导块——当 `completeness: partial` 时展示微信联系人 / 新生群提示，鼓励用户联系补充；在 `app/globals.css` 新增 `.supplement-card / .supplement-actions / .supplement-action` 样式。
- **权限阻塞资料**（04、37）走网络公开信息整理：
  - `docs/05-校园生活/湘潭地铁乘坐指南.md`（长株潭城铁 / 长沙地铁 / 西环线）
  - `docs/02-课程学习/培养计划.md`（培养方案通用介绍 + 官方入口）
  - 均标 `source: 网络公开资料整理`、`completeness: partial`。
- **完整正文 6 篇入正式库**：`docs/04-学院专业/学校简介与校史.md`、`docs/04-学院专业/院系与专业设置.md`、`docs/05-校园生活/图书馆与自习室.md`、`docs/06-办事指南/奖学金与助学金政策.md`（合并 07/12 号具体数字）、`docs/02-课程学习/保研与科学竞赛.md`、`docs/02-课程学习/实验班与拔尖班选拔.md`。
- **正文/附件不足 12 篇入库**：`23-校园文化.md`、`25-湘大校园地图.md`、`26-湘大校园实拍.md`、`27-作息安排.md`、`28-选课与通识课.md`、`30-转专业指南.md`、`31-常见考证清单.md`、`33-教学楼分布.md`、`34-湘大教务系统使用入门.md`、`35-学分成绩与课程考核.md`、`36-选课修课与访学.md`、`40-校园常见办事流程.md`；均按 `completeness: partial` 触发末尾引导块。
- **05–20 号资料入库**：`开学与迎新日程.md`、`2026新生报到流程.md`、`大学概念百科.md`、`开学物品指南.md`、`军训指南.md`、`买电脑分享.md`、`新生报到QA.md`、`校园政策QA.md`（06-办事指南）、`升学就业QA.md`（02-课程学习）、`实用工具小程序.md`、`健康体魄勤运动.md`、`快递收发驿站通.md`、`寝室生活大揭秘.md`、`校园出行有攻略.md`、`学校里的美食家.md`（标 partial：原文档链接与 21 号医保冲突导致内容错位）、`湘潭游玩指南.md`（标 partial：原文档正文过短）、`医保就医超优惠.md`。跳过 09-2026 招生方案（原文仅省名列表）。
- **41–60 号资料入库**：`信息门户使用指南.md`、`校园电话卡与寝室宽带办理.md`（06-办事指南）、`学校网络使用指南.md`、`VPN使用指南.md`、`校园一卡通指南.md`、`线上缴费指南.md`、`故障申报与受理.md`、`湘大社团一览.md`（合并 49-55 号学术/公益/实践/文化/科技/艺术/体育七大类）、`班委职务与竞选.md`、`湘潭大学学生会.md`、`湘潭大学大学生艺术团.md`、`湘潭大学学生社团联合会.md`、`湘潭大学研究生会介绍.md`。
- 合计新增正式资料约 **42 篇**，覆盖 60 条待整理来源。
- 遵守资料来源红线：涉及网络公开整理的资料统一在 `source` 字段和 `completenessNote` 中标注，并在页面末尾引导用户补充。

### 验证状态

- `npm --prefix "C:\xtuer-zhou" run rag:index` 通过：**RAG 索引从 34 chunks 增长到 762 chunks**。
- `npm --prefix "C:\xtuer-zhou" run build` 通过：Next.js 15.5.7 编译成功，静态页面从 16 张增长到 **66 张**。
- 页面结构与 `/library/[...slug]` 详情页路由匹配良好，未发现路由报错。

### 当前状态

60 条腾讯文档来源全部完成 “**待整理 → 正式资料**” 迁移；同时详情页在 `completeness: partial` 时自动展示 “补充信息 + 微信 / 新生群” 引导块，为后续用户共建留出入口。RAG 索引已按新资料重建。下一步建议：
1. 移动端体验验收（尤其详情页 `.supplement-card` 卡片在小屏上的排版）与部署测试准备（Vercel 或类似平台的 DeepSeek Key 注入）。
2. 04-湘潭地铁乘坐、37-培养计划、以及标注 `partial` 的资料后续获取到官方素材（图片、附件、时效政策）后逐步升级为 `complete`。

## 2026-07-17 腾讯文档 22–40 号全量补存与状态核验

### 用户输入

- 用户要求先保存腾讯文档清单中的 22–40 号资料。

### 本次执行

- 逐条访问 `docs/99-待整理资料/腾讯文档链接清单.json` 中 22–40 号来源，将每条提取结果保存至 `docs/99-待整理资料/腾讯文档下载/`。
- 本轮新增 19 篇待整理 Markdown；加上已补存的 21 号，60 条来源清单现均有对应 Markdown 记录。
- 更新 `权限不足与待补清单.md`：新增 37 号培养计划的权限阻塞项，并把 23、25–28、30–31、33–36、40 号记录为正文、附件或媒体不完整。

### 结果概览

- 可读取一定正文、仍待审核：22、24、29、32、38、39。
- 页面可访问但正文/附件/媒体不足：23、25、26、27、28、30、31、33、34、35、36、40。
- 明确权限不足：37-培养计划；原有 04-湘潭地铁乘坐仍保持权限不足。
- 所有本轮资料均保留在待整理区，未写入正式资料目录、未加入 RAG 索引、未改动前台内容。

### 当前状态

下载覆盖已从 41/60 推进至 60/60 条来源均有本地记录；但“有记录”不等于“资料可用”。下一步应先补齐 04、37 的权限和附件/媒体缺口，再以来源可追溯、正文完整、人工审核通过为门槛，小批量拆分正式资料。

## 2026-07-17 腾讯文档待整理资料补存与权限清单

### 用户输入

- 用户要求阅读 `C:\xtuer-zhou` 项目和最近的 Claude 聊天记录，确认项目进展。
- 用户要求继续完成文档保存和权限不足名单获取。

### 本次执行

- 已阅读项目状态、开发日志、路线图和最近 Claude 聊天记录，确认项目仍处于“内容扩充与检索质量优化 / 资料详情卡片继续打磨与部署测试准备”阶段。
- 已对照 `docs/99-待整理资料/腾讯文档链接清单.json` 与 `docs/99-待整理资料/腾讯文档下载/` 已保存 Markdown，确认链接清单共 60 条。
- 已补存 `docs/99-待整理资料/腾讯文档下载/21-医保就医超优惠.md`，该资料仍位于待整理区，未进入正式资料库或 RAG 索引。
- 已新增 `docs/99-待整理资料/腾讯文档下载/权限不足与待补清单.md`，记录明确权限不足、图片加载失败、正文过短和仍待保存条目。

### 权限与待补结论

- 明确权限不足：`04-湘潭地铁乘坐`，页面只返回“已申请编辑权限 / 请等待文档创建人 Renaissance 处理”，需等待创建人通过后重试。
- 已保存但需复核：`15-健康体魄勤运动`、`51-实践类`、`54-艺术类`、`55-体育类` 存在图片加载失败；`20-湘潭的游玩指南` 正文过短。
- 尚未保存为 Markdown：`22-学校简介及校史` 至 `40-办事流程与指南` 共 19 条。

### 验证状态

- 本轮主要是资料保存和清单整理，未改动前台代码，未运行 `npm run build`。
- 已通过文件扫描和下载结果 JSON 对照确认已保存数量与待补范围。

### 当前状态

腾讯文档下载工作已从“零散保存”整理为“已保存 + 明确权限不足 + 待补清单”的可继续推进状态。下一步建议优先补齐 22-40 号文档，再决定哪些内容经过人工审核后进入正式资料库。

## 2026-07-16 资料库非检索卡片跳转修复

### 用户输入

- 用户反馈：检索结果里的参考卡片可以正常跳转，但资料库里其他卡片不能正常跳转。

### 本次执行

- 已定位问题：问答来源卡片使用了明确的资料详情链接，而资料库的主题卡片原本只是展示卡片，没有跳转链接；资料列表卡片虽然标题和按钮可点，但卡片主体不可点，用户容易误以为“卡片不能跳转”。
- 已更新 `app/library/page.tsx`：主题导航卡片现在会关联该主题下第一篇指南，并显示“查看这个主题”入口；资料列表卡片增加整卡可点击链接。
- 已更新 `app/library/[...slug]/page.tsx`：详情页底部“你可能还想看”推荐卡片也支持整卡点击。
- 已更新 `app/globals.css`：新增 `.clickable-card` 与 `.card-overlay-link`，让资料卡片整体具备点击区域和手型反馈。

### 验证状态

- 已运行 `npm run build`，构建通过。
- 已用 Playwright 打开 `http://localhost:3003/library`，点击资料列表中的“新生攻略总览”卡片主体，成功跳转到详情页。
- 已用 Playwright 点击主题导航中的“课程学习”，成功跳转到 `学在湘大总览` 详情页。

### 当前状态

资料库中的主题卡片、资料列表卡片、问答来源卡片、详情页推荐卡片均已具备可跳转能力。当前本地服务地址仍为 `http://localhost:3003`。

## 2026-07-16 DeepSeek 回答优化与来源跳转复验

### 用户输入

- 用户反馈资料页卡片仍不能正确跳转。
- 用户确认 RAG 检索本身没有问题，但输出需要先经过 AI 优化，再生成一段更口语化的表达。
- 用户提供 DeepSeek API Key，要求开始接入。

### 本次执行

- 已将 DeepSeek Key 写入 `C:/xtuer-zhou/.env.local`，该文件已被 `.gitignore` 排除，不会进入 Git 提交。
- 已新增 `lib/deepseek.ts`，在本地检索命中资料后调用 DeepSeek，将检索片段整理成更自然、口语化、适合新生阅读的回答。
- 已更新 `app/api/ask/route.ts`：流程变为“先本地检索 → 检索不到则直接兜底 → 检索到资料后调用 DeepSeek 优化表达 → 失败时回退到本地拼接答案”。
- 已更新 `lib/routes.ts` 和 `components/ask-panel.tsx`，统一从来源文件路径生成资料详情链接，避免问答来源跳转路径不一致。
- 已隐藏问答来源卡片中的原始文件路径，只保留更新时间与整理来源，减少开发者视角信息。

### 验证状态

- 已运行 `npm run build`，构建通过。
- 已重启本地服务，当前访问地址为 `http://localhost:3003`。
- 已用 Playwright 在首页提问“新生群是什么？”，返回结果为 DeepSeek 优化后的口语化回答。
- 已点击问答结果中的第一条参考内容，成功跳转到资料详情页 `/library/01-新生入学/公告栏与新生群提醒`，页面正常显示，不是 404。

### 当前状态

当前问答能力已经升级为“本地资料检索 + DeepSeek 口语化生成 + 来源卡片跳转”。DeepSeek 只在检索到资料后参与表达优化，答案仍受本地资料约束；如果模型调用失败，会自动回退到本地答案，保证功能可用。

## 2026-07-16 资料跳转修复与学生化前端体验优化

### 用户输入

- 用户反馈整体 UI 语言偏开发者视角，不像面对学生使用，需要去掉资料状态、索引等内部表达。
- 用户反馈资料库点击进入后出现 404，当前应重点开发跳转后的资料卡片与正文显示。
- 用户确认腾讯文档中的资料全部经过审核、信息无误。
- 用户要求梳理当前 RAG 检索使用什么模型，并说明稍后会提供 DeepSeek API。

### 本次执行

- 已新增 `lib/routes.ts`，统一生成中文资料详情页链接，避免中文路径未编码导致跳转不稳定。
- 已增强 `lib/markdown.ts` 的 slug 规范化逻辑，详情页可兼容 URL 编码后的中文路径。
- 已新增 `lib/display.ts`，集中处理前台展示用分类名、来源名和摘要清理，避免把 Markdown 标记和内部路径直接展示给学生。
- 已重做 `app/library/page.tsx` 的前台语言：资料库改为“新生指南资料库”，过滤项目说明类文档，隐藏 `docs/`、`已审核`、`允许索引` 等开发者字段。
- 已重做 `app/library/[...slug]/page.tsx` 的详情页展示：顶部改为学生可读资料卡片，显示主题、标题、摘要、更新时间和整理来源；同类推荐改为“你可能还想看”。
- 已调整 `components/ask-panel.tsx`、`app/page.tsx`、`app/guide/page.tsx`、`components/contact-dialog.tsx`、`app/layout.tsx` 的文案，移除前台 `RAG`、`已审核`、`允许索引`、`indexable`、`docs/`、`chunk` 等开发术语。
- 已更新 `app/globals.css`，补充详情页资料卡片、移动端问答表单、卡片 hover、前台提示卡片等样式。
- 已确认当前 RAG 没有调用任何大模型：`lib/rag.ts` 仍是本地关键词检索、评分和片段拼接；DeepSeek API 后续可作为生成层接入。

### 验证状态

- 已运行 `npm run build`，Next.js 构建通过，并继续生成 16 个静态页面。
- 已启动本地开发服务，当前可访问地址：`http://localhost:3002`。
- 已用 Playwright 打开资料详情页 `/library/01-新生入学/公告栏与新生群提醒`，页面返回 200，不再是 404，标题和正文均可显示。
- 已用搜索确认前台 TSX/CSS 中不再出现 `RAG`、`已审核`、`允许索引`、`indexable`、`docs/`、`chunk`、`本地索引`、`资料状态` 等开发者术语。
- 验证过程中发现旧 dev 服务热更新后 `.next` chunk 一度失效导致 500；已停止旧服务并在 3002 端口重新启动，详情页恢复 200。

### 当前状态

资料库到详情页的核心跳转已经修复，前台语言已经从开发者视角切换为学生视角。当前问答仍是本地检索式能力，没有使用 DeepSeek 或其他模型；下一步建议先继续完善资料详情页内容卡片与更细颗粒度资料拆分，再接入 DeepSeek 作为基于检索结果的回答生成层。

## 2026-07-16 RAG 问答结果结构轻量优化

### 用户输入

- 用户要求阅读 `C:\xtuer-zhou` 项目状态并继续推进。

### 本次执行

- 已阅读 `README.md`、`product/development-log.md`、`product/development-roadmap.md`、`product/PRD.md`，确认当前仍处于内容扩充与检索质量优化阶段。
- 已优化 `lib/rag.ts` 的回答结构：新增统一片段截断、来源行格式化、命中 token 数记录和同分排序补充。
- 已将 RAG 回答结尾改为可信提示，明确“仅引用本地已审核资料”，避免用户误认为是实时官方信息。
- 已优化 `components/ask-panel.tsx` 的引用来源卡片，展示来源序号、标题、命中标题层级、短摘录、路径、更新时间和来源。
- 已更新 `app/globals.css`，补充来源区域标题与摘录样式，复用现有卡片结构，没有新增复杂组件或依赖。

### 验证状态

- 首次在非项目目录运行 `npm run rag:index && npm run build` 失败，原因是当前 shell 工作目录不在 `C:/xtuer-zhou`，未找到 `package.json`。
- 已改为在 `C:/xtuer-zhou` 项目目录执行 `npm run rag:index && npm run build`，RAG 索引重新生成 34 个 chunks，Next.js 构建通过。
- 额外尝试直接 require `.next` 构建产物测试检索函数失败，原因是构建 chunk 路径不稳定；该尝试不影响已通过的正式构建验证。

### 当前状态

RAG 问答结果已经从“纯文本堆叠”小幅升级为“回答摘要 + 结构化来源卡片 + 可信提示”。本轮没有新增未审核资料，没有引入新依赖，也没有扩大页面范围，继续遵守轻量开发约束。下一步建议优先做两件事之一：继续拆分少量高价值新生攻略细颗粒度资料，或对 RAG 问答加入更明确的低置信度阈值与问题改写提示。

## 2026-07-16 推送 GitHub 首版完成

### 用户输入

- 用户要求先推送到 GitHub，并提供远程仓库 `https://github.com/qing99half/xtuer-zhou.git`。

### 本次执行

- 已初始化本地 Git 仓库。
- 已创建 `.gitignore`，排除 `node_modules/`、`.next/`、环境文件和原始 `.docx` 等不应提交内容。
- 已按用户确认，不包含原始腾讯文档导出 `.docx`。
- 已设置本仓库 Git 提交身份：`qing99half <a13165566223@163.com>`。
- 已完成首版提交：`6863853 feat: initialize xtuer zhou knowledge base`。
- 已添加远程仓库并推送到 GitHub。
- 由于远程仓库已有初始化提交，用户选择使用 `--force` 覆盖远程 main 分支。
- 当前本地 `main` 已跟踪 `origin/main`，状态干净。

### 当前状态

项目首版已经推送到 GitHub：`https://github.com/qing99half/xtuer-zhou`。当前仍处于内容扩充与检索质量优化阶段，后续开发应继续小步迭代，避免代码和资料臃肿。

## 2026-07-16 轻量开发约束与进度汇报规则确认

### 用户输入

- 用户要求思考约束，避免过多内容导致代码和内容臃肿。
- 用户要求每次都说明当前开发进程，并继续开发。

### 本次执行

- 已在 `product/PRD.md` 新增“轻量开发与汇报约束”，明确小步迭代、避免重复组件、避免一次性入库过多内容、RAG 优先轻量优化等规则。
- 已在 `product/development-roadmap.md` 新增“当前开发进程汇报规则”和“轻量开发约束”。
- 已轻量优化 `lib/rag.ts` 的检索评分：标题、标题层级、分类、正文采用不同权重，减少无关内容靠前。
- 已限制 RAG 回答片段长度，避免答案输出过长导致页面和内容臃肿。

### 当前状态

当前项目处于“内容扩充与检索质量优化阶段”。本轮没有新增大量资料或新组件，只补充开发约束并对现有 RAG 逻辑做小幅增强。后续每次回复都会明确当前进程、完成内容、验证状态和下一步建议。

## 2026-07-16 资料详情页体验优化与来源高亮

### 用户输入

- 用户确认继续推进，优化资料详情页 Markdown 渲染、来源定位高亮和同类资料推荐。

### 本次执行

- 已重构 `app/library/[...slug]/page.tsx` 的 Markdown 渲染逻辑，支持标题、段落、无序列表、有序列表。
- 已为标题生成稳定锚点，支持 RAG 来源跳转定位。
- 已让 RAG 来源链接携带 `highlight` 参数，详情页可对命中片段进行高亮显示。
- 已在资料详情页底部增加同分类资料推荐。
- 已更新 `app/globals.css`，补充列表样式和高亮命中样式。
- 已运行 `npm run rag:index`，重新生成 34 个 RAG 片段。
- 已运行 `npm run build`，构建通过，资料详情静态页面继续正常生成。

### 当前状态

资料详情页阅读体验、RAG 来源定位和同类资料推荐已完成。下一步建议继续扩充更细颗粒度的新生攻略资料，或优化 RAG 检索评分与问答结果结构。

## 2026-07-16 资料详情页与来源链接完成

### 用户输入

- 用户要求继续做资料详情页和来源可点击跳转能力。

### 本次执行

- 已新增 `app/library/[...slug]/page.tsx`，为已审核 Markdown 资料生成详情页。
- 已增强 `lib/markdown.ts`，支持按 slug 读取单篇已审核资料。
- 已更新 `app/library/page.tsx`，资料库卡片标题和“查看全文”按钮可跳转资料详情页。
- 已更新 `components/ask-panel.tsx`，RAG 回答中的引用来源可点击跳转到对应资料详情页，并携带标题锚点。
- 已更新 `app/globals.css`，补充资料详情正文与来源链接样式。
- 已运行 `npm run rag:index`，重新生成 34 个 RAG 片段。
- 已运行 `npm run build`，构建通过，并生成 9 个资料详情静态页面。

### 当前状态

资料库和 RAG 来源已经形成闭环：用户可以从资料库进入详情页，也可以从首页问答结果点击来源跳转到对应资料。下一步建议继续做资料详情页的 Markdown 渲染体验优化、来源句子高亮，或继续扩充更细颗粒度的新生攻略资料。

## 2026-07-16 联系方式占位与问答体验增强

### 用户输入

- 用户说明交流群后续会使用微信二维码，联系人也同样使用微信二维码。
- 当前先使用占位，并继续推动开发。

### 本次执行

- 已扩展 `lib/site-data.ts` 的联系方式结构，支持 `qr`、`group`、`link` 类型。
- 已将联系方式调整为：交流群微信二维码占位、联系人微信二维码占位、QQ 新生群、湘大表白墙。
- 已更新 `components/contact-dialog.tsx`，在联系方式弹窗中展示二维码占位区域。
- 已更新 `app/page.tsx`，首页咨询入口同步展示二维码占位区域。
- 已更新 `components/ask-panel.tsx`，为首页问答入口增加示例问题快捷按钮。
- 已更新 `app/globals.css`，补充二维码占位和示例问题按钮样式。
- 已运行 `npm run rag:index`，重新生成 34 个 RAG 片段。
- 已运行 `npm run build`，构建通过。

### 当前状态

联系方式展示已经为后续真实二维码素材预留结构。首页问答入口已有示例问题按钮，RAG 本地闭环继续保持可用。下一步建议继续补充真实联系方式素材，或优化 RAG 检索质量与资料详情页跳转。

## 2026-07-16 RAG 本地验证闭环首版完成

### 用户输入

- 用户要求继续开发，进入 Phase 4：RAG 本地验证闭环。

### 本次执行

- 已新增 `scripts/build-rag-index.mjs`，扫描 `status: 已审核` 且 `indexable: true` 的 Markdown 文档，并生成本地索引。
- 已新增 `data/rag-index.json`，当前生成 34 个资料片段。
- 已新增 `lib/rag.ts`，实现本地关键词检索、资料不足兜底、来源返回。
- 已新增 `app/api/ask/route.ts`，提供本地问答检索接口。
- 已新增 `components/ask-panel.tsx`，将首页 AI 问答入口从占位改为可输入、可提交、可展示答案和来源。
- 已更新 `app/page.tsx`，首页接入问答组件。
- 已更新 `app/globals.css`，补充问答结果、错误提示和来源卡片样式。
- 已新增 `npm run rag:index` 命令。
- 已运行 `npm run rag:index`，生成 34 个 chunk。
- 已运行 `npm run build`，构建通过。
- 已用“新生群是什么”测试本地检索函数，能够返回新生群、信息甄别提醒和群聊使用建议，并附带来源。

### 当前状态

RAG 本地验证闭环首版已完成。当前能力是关键词检索式本地 RAG，不调用外部模型，不编造答案；后续可以继续优化检索评分、接入真实生成模型、增加句子级高亮和更多已审核资料。

## 2026-07-16 首批新生攻略资料入库

### 用户输入

- 用户确认“先入库”，要求将已整理的新生攻略内容优先进入正式资料库。

### 本次执行

- 已基于用户确认入库的《湘潭大学2026新生攻略》可见内容，拆分首批正式 Markdown 资料。
- 已新增 `docs/01-新生入学/新生攻略总览.md`。
- 已新增 `docs/01-新生入学/公告栏与新生群提醒.md`。
- 已新增 `docs/02-课程学习/学在湘大总览.md`。
- 已新增 `docs/04-学院专业/湘大印象总览.md`。
- 已新增 `docs/05-校园生活/校园生活总览.md`。
- 已新增 `docs/05-校园生活/社团与组织总览.md`。
- 已新增 `docs/07-资源工具/工具资源总览.md`。
- 已将对应资料标记为 `status: 已审核`、`indexable: true`，作为后续资料库展示和 RAG 本地验证的第一批内容。
- 已更新资料库分类状态，让新生入学、课程学习、学院专业、校园生活、资源工具等分类展示为已审核。
- 已运行 `npm run build`，构建通过。

### 当前状态

首批新生攻略资料已进入正式资料库。下一步建议进入 Phase 4：RAG 本地验证闭环，基于 `status: 已审核` 且 `indexable: true` 的 Markdown 文档构建本地索引与问答接口。

## 2026-07-16 Markdown 内容体系与资料库展示增强

### 用户输入

- 用户要求继续开发，进入 Phase 3：Markdown 内容模板与已审核资料迁移。

### 本次执行

- 已创建正式资料目录：`docs/00-知识库说明/`、`docs/01-新生入学/` 等核心栏目。
- 已新增 `docs/00-知识库说明/Markdown资料模板.md`，明确正式资料 frontmatter 字段与正文要求。
- 已新增 `docs/00-知识库说明/资料入库与审核规则.md`，记录“先待整理、再审核、后入库”的流程与红线。
- 已新增 `docs/01-新生入学/README.md` 作为待整理占位，不进入正式展示。
- 已增强 `lib/markdown.ts`，支持读取 `indexable`、生成摘要，并继续只返回 `status: 已审核` 的资料。
- 已增强资料库页面，展示资料分类、更新时间、来源与是否允许索引。
- 已运行 `npm run build`，构建通过。

### 当前状态

Phase 3 的基础内容体系已完成。当前资料库已经能展示首批“项目规则类已审核资料”，而用户提供的新生攻略仍保留在待整理区。下一步建议继续确认哪些新生攻略栏目可以入库，或开始 Phase 4：RAG 本地验证闭环。

## 2026-07-16 Next.js 工程初始化与基础骨架完成

### 用户输入

- 用户要求继续开发，按 `product/development-roadmap.md` 从 Phase 1 开始推进。
- 本阶段需要完成 Next.js 工程初始化、首页、入学指南、资料库、顶部导航、联系方式弹窗和 Markdown 内容读取基础结构。

### 本次执行

- 已完成 UI 设计方案文档 `product/ui-design.md`，明确年轻活泼、实用知识库风格、导航、弹窗、首页、指南页和资料库布局规则。
- 已完成 Next.js + React + TypeScript 基础工程初始化。
- 已建立基础路由：`/`、`/guide`、`/library`。
- 已实现全站布局：顶部导航、页脚、联系方式弹窗。
- 已实现首页骨架：新生入学路线、AI 问答入口占位、咨询入口二次强调。
- 已实现入学指南统一模板骨架。
- 已实现资料库分类导航与“只展示已审核资料”的空状态。
- 已建立 `lib/markdown.ts`，后续可从 Markdown 读取已审核资料。
- 已运行 `npm run build`，构建通过。

### 当前状态

Phase 1 工程初始化与基础骨架已完成，Phase 2 的核心页面首版也已具备可演示结构。下一步建议进入 Phase 3：Markdown 内容模板与已审核资料迁移；随后推进 Phase 4：RAG 本地验证闭环。

## 2026-07-15 接收整理文档并启动开发排期

### 用户输入

- 用户提供腾讯文档链接，说明该文档为已整理好的《湘潭大学2026新生攻略》。
- 用户要求下载该文档，并开始进行开发任务排期。

### 本次执行

- 已通过腾讯文档页面导出 Word 文件，并保存为 `docs/99-待整理资料/湘潭大学2026新生攻略.docx`。
- 已基于页面可见内容和 DOCX 初步提取生成 `docs/99-待整理资料/湘潭大学2026新生攻略-提取文本.md`。
- 已创建开发排期文档 `product/development-roadmap.md`，覆盖 MVP 范围、阶段拆分、优先级、里程碑、风险与下一步待确认事项。
- 资料仍处于待整理 / 待审核状态，未进入正式资料库或 RAG 索引。

### 当前状态

项目已从 PRD 轮询阶段进入开发排期阶段。下一步可在确认资料审核范围和联系方式素材后，启动 Next.js 工程初始化；也可以继续 Round 11：RAG 第一版交互与回答规则确认。

## 2026-07-15 PRD Round 10：联系方式内容与展示规则确认

### 用户选择

- 顶部导航联系方式展示方式：文字入口 + 二维码弹窗。
- QQ 新生群和湘大表白墙：首页卡片中二次强调。
- 联系方式单独页面：第一版暂不设置。

### 结论

全站顶部导航使用轻量文字入口承载联系方式，点击后通过弹窗集中展示微信二维码、QQ 新生群、湘大表白墙。首页需要用卡片或模块二次强调社群和咨询入口，帮助新生在浏览入学路线时快速找到咨询渠道。第一版不新增独立联系页面，保持 MVP 页面结构简洁。

### 文档更新

- 已更新 `product/PRD.md` 的“Round 10：联系方式内容与展示规则”。
- 已将下一轮轮询推进到 Round 11：RAG 第一版交互与回答规则。

## 2026-07-14 初始化

### 用户确认

- 项目是湘潭大学学习文档知识库。
- 不继续进行腾讯文档下载和爬虫采集。
- 仅保留知识库目录结构。
- 进入 PRD 确认轮询环节。
- 每次轮询确认后，都需要更新开发文档。

### 本次执行

- 清理早期采集脚本与调研文件。
- 删除 D 盘临时采集目录 `D:/xtuer-zhou-data`。
- 创建知识库目录结构。
- 创建 `product/PRD.md`。
- 创建 `product/development-log.md`。

### 当前状态

PRD 轮询已按用户要求暂停。下一步等待用户补充资料或重新启动确认流程。

## 2026-07-14 暂停 PRD 轮询并修正学校名称

### 用户要求

- 先停止 PRD 轮询。
- 记录开发文档。
- 将学校从“湖南大学”修正为“湘潭大学”。
- 写入约束：禁止在开发过程中使用未经确认的网络信息来源写入存储。

### 本次执行

- 已将 `README.md`、`product/PRD.md`、`product/development-log.md` 中的学校名称统一修正为“湘潭大学”。
- 已在 `product/PRD.md` 新增“开发约束与资料来源规范”。
- 已明确禁止未经用户确认的网络信息进入项目存储、正式资料库或 RAG 索引。
- 已暂停 PRD Round 10 轮询，等待后续用户补充资料或重新启动确认。

### 资料来源红线

开发过程中不得将搜索引擎、网页、论坛、公众号、社群截图等未经用户确认的网络信息直接写入正式资料库、项目存储或 RAG 索引。外部资料必须先进入待整理区，经过用户审核确认后，才允许进入正式资料库。

## 2026-07-14 PRD Round 9：页面信息架构与内容状态确认

### 用户选择

- 资料页面内容状态：已审核
- 入学指南 4 个模块模板：统一模板
- 资料库分类导航：沿用目录

### 结论

资料页面第一版只展示“已审核”状态，所有进入正式资料库与 RAG 索引的资料都必须先由用户审核。入学指南 4 个模块采用统一模板，保证体验一致和维护简单。资料库分类第一版沿用当前本地目录结构，减少文件维护、网页导航和 RAG 入库之间的映射成本。

### 文档更新

- 已更新 `product/PRD.md` 的“Round 9：页面信息架构与内容状态”。
- 已将下一轮轮询推进到 Round 10：联系方式内容与展示规则。

## 2026-07-14 PRD Round 8：资料文件结构与引用规范确认

### 用户选择

- 资料正文第一版文件结构：Markdown
- 引用来源粒度：句子级
- 资料入库审核：所有资料由用户先行审核后再入库

### 结论

第一版资料正文采用 Markdown 文件维护，以降低录入与整理成本。RAG 引用需要精确到句子级，答案应尽量可追溯到具体原文句子。所有资料必须先由用户审核，通过后才进入正式资料库与 RAG 索引；未审核资料先放入 `docs/99-待整理资料/`。

### 文档更新

- 已更新 `product/PRD.md` 的“Round 8：资料文件结构与引用规范”。
- 已将下一轮轮询推进到 Round 9：页面信息架构与内容状态。

## 2026-07-14 PRD Round 7：技术路线与开发边界确认

### 用户选择

- 网页站点技术栈：Next.js
- RAG 服务部署方式：先本地后云端
- 第一版资料维护方式：文件维护

### 结论

第一版采用 Next.js 构建网页站点，RAG 服务走“先本地验证、再云端部署”的路线。资料维护采用文件维护方式，优先保证个人维护下的内容质量、结构统一和快速迭代。后台管理页面暂不进入第一版刚需，但资料结构需要保持规范，方便后续迁移到后台或 CMS。

### 文档更新

- 已更新 `product/PRD.md` 的“Round 7：技术路线与开发边界”。
- 已将下一轮轮询推进到 Round 8：资料文件结构与引用规范。

## 2026-07-14 PRD Round 6：RAG 第一版能力边界确认

### 用户选择

- RAG 第一版资料来源范围：全部资料库
- RAG 第一版问题入口：首页主入口
- RAG 回答失败或资料不足时：引导添加/使用联系方式

### 结论

RAG 第一版覆盖资料库中的全部已整理资料，并作为首页主入口展示。资料库内容必须按可检索、可引用、可追溯的标准管理。若资料不足或无法回答，系统不能编造答案，需要提示资料库暂未覆盖，并引导用户通过微信二维码、QQ 新生群、湘大表白墙等联系方式咨询或补充资料。

### 文档更新

- 已更新 `product/PRD.md` 的“Round 6：RAG 第一版能力边界”。
- 已将下一轮轮询推进到 Round 7：技术路线与开发边界。

## 2026-07-14 PRD Round 5：站点导航、首页结构与 RAG 要求确认

### 用户选择

- 联系方式展示方式：顶部导航
- 首页“新生入学路线”呈现方式：模块卡片
- RAG 检索入口处理：第一版必须完成，不只是预留或建设中
- 技术支撑：后续开发中引用 `raganything` 项目
- 项目节奏：允许投入一定开发时间完成 RAG 能力

### 结论

全站联系方式统一放在顶部导航栏。首页新生入学路线采用模块卡片组织，围绕报到流程、宿舍生活、军训指南、学习入门四个核心模块展开。RAG 检索能力升级为第一版核心功能，后续将以 `raganything` 作为重要技术支撑，必须实现基于资料的检索、回答生成和引用溯源；资料不足时不能编造答案。

### 文档更新

- 已更新 `product/PRD.md` 的“Round 5：站点导航、首页结构与 RAG 要求”。
- 已将下一轮轮询推进到 Round 6：RAG 第一版能力边界。

## 2026-07-14 PRD Round 4：网页站点范围确认

### 用户选择

- 第一版页面范围：首页、入学指南、资料库
- 补充要求：每个页面必须在标题栏或其他显眼位置放置联系方式，包括微信二维码、QQ 新生群、湘大表白墙
- 首页重点：新生入学路线
- 后续项目亮点：补充 RAG 检索
- 视觉风格：年轻活泼，后续开发过程中需要详细对齐

### 结论

网页站点第一版围绕新生入学场景落地：首页负责呈现新生入学路线，入学指南负责承载 4 个 MVP 内容模块，资料库负责沉淀后续扩展内容。全站需要显眼展示联系方式和社群入口。RAG 检索暂作为后续亮点纳入规划。视觉风格采用年轻活泼路线，但 UI 细节需要在开发过程中持续确认。

### 文档更新

- 已更新 `product/PRD.md` 的“Round 4：网页站点范围”。
- 已将下一轮轮询推进到 Round 5：站点导航与联系方式。

## 2026-07-14 PRD Round 3：MVP 交付形态确认

### 用户选择

- MVP 第一版交付形态：网页站点
- AI 问答能力：现在规划
- 内容更新权限：个人维护

### 结论

MVP 第一版以网页站点落地，而不是仅保留 Markdown 目录。AI 问答能力从现在开始做底层规划，包括资料结构、引用规范、问答卡片格式和内容元数据；但第一版不强制完整实现 AI 问答。内容维护权限采用个人维护，先保证质量和统一性。

### 文档更新

- 已更新 `product/PRD.md` 的“Round 3：MVP 交付形态”。
- 已将下一轮轮询推进到 Round 4：网页站点范围。

## 2026-07-14 PRD Round 2：内容范围确认

### 用户选择

- MVP 第一批内容模块：学习入门、军训指南、宿舍生活、报到流程
- 内容呈现形式：两者结合
- AI 问答引用要求：必须引用

### 结论

MVP 新生入学指南先覆盖报到流程、宿舍生活、军训指南、学习入门 4 个模块。内容采用“长文指南 + 问答卡片”的双层结构，兼顾系统阅读和快速检索。后续 AI 问答必须引用资料原文或明确来源，保证可信度。

### 文档更新

- 已更新 `product/PRD.md` 的“Round 2：内容范围”。
- 已将下一轮轮询推进到 Round 3：MVP 交付形态。

## 2026-07-14 PRD Round 1：产品定位确认

### 用户选择

- 第一目标用户：新生优先
- MVP 阶段最重要使用场景：入学指南
- 最终形态：AI 问答库

### 结论

项目优先服务湘潭大学新生，先解决入学阶段的信息差问题。MVP 以入学指南为核心，后续在结构化资料基础上升级为 AI 问答知识库。

### 文档更新

- 已更新 `product/PRD.md` 的“已确认需求”。
- 已将下一轮轮询推进到 Round 2：内容范围。
